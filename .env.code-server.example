# Copy to .env.development.local for pnpm dev.
# APP_ORIGIN is optional if code-server supplies VSCODE_PROXY_URI.
APP_ENV=code-server
APP_BASE_PATH=/absproxy/3000
PORT=3000
APP_ORIGIN=
DEV_ALLOWED_HOSTS=
