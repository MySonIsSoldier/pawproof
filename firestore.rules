rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the authenticated Next.js server accesses Firestore via Admin IAM.
    match /{document=**} { allow read, write: if false; }
  }
}
