# 최근 작업 로그

상세 구현 이력은 [구현 기록](IMPLEMENTATION_STATUS.md), 결정 근거는 [결정 이력](../history/DECISION_HISTORY.md)에 둔다. 다음 작업자는 이 문서와 루트 AGENTS.md부터 확인한다.

## 2026-09-13 · Vercel 배포와 PWA Google 로그인

- 사용자 확인: `https://pawproof-rose.vercel.app/` 첫 배포 및 환경변수 설정 완료. 이후 검증된 변경의 `main` 푸시·Vercel 자동 배포를 에이전트가 진행하도록 승인했다.
- 배포 준비: 운영 `.env.example` 기본값·직접 입력 10개를 정리했다(`80238be`). Firebase 개발 도구의 선택적 re2 빌드 정책 누락을 수정하고 새 설치·운영 빌드를 검증했다(`204f985`).
- 제보: iPhone Safari에서 설치한 PWA의 Google 로그인 창에 키보드가 뜨지 않음. 수정 전 운영 브라우저 검사에서 OAuth 창을 열 때 앱 모달의 포커스/스크롤 잠금이 남는 것을 확인했다. 이것만으로 OS 키보드 문제의 단일 원인이 확정된 것은 아니다.
- 조치: Google 로그인 훅으로 흐름을 분리하고 팝업 전에 모달을 완전히 제거한다. Auth SDK를 준비 단계에서 로드해 클릭 중 동적 import를 없앴다. 성공 시 `/plan`·토스트, 취소/차단 시 모달 복귀를 유지한다. iOS가 창 닫기를 감지하지 못할 때를 위한 대기 종료 버튼과 재시도 경합 방지를 추가했다.
- 문서: AGENTS.md의 현재 상태·코드 품질·비밀 관리·검증·푸시/자동 배포 규칙을 갱신했다. [Firebase 계약](../engineering/FIREBASE_AUTH_AND_STORAGE.md)과 [반복 검사](../engineering/TESTING.md)에 동작과 실행법을 기록했다.

검증: 관련 Playwright 26개(기존 Firebase 18개 + Google 팝업 6개 + iOS 신호 경로 2개), 단위 91개, lint·typecheck·운영 빌드 통과. iOS 신호 검사의 최초 시간 초과는 앵커로 열린 창을 일반 popup 이벤트로 기다린 검사 오류였으며 context의 새 page 이벤트로 수정해 통과했다. 운영 점검 스크립트는 실제 Google 창을 열고 닫기만 하며 사용자 자격증명을 입력하지 않는다. 개발 미리보기는 검사 후 재시작했다.

운영 반영: 로그인 수정 `a90c3cb`와 문서 갱신 `d940bb8`을 main에 푸시했다. 이후 운영 `/sw.js`의 새 릴리스 반영과 홈 HTTP 200을 확인했다. 같은 운영 점검 스크립트에서 OAuth 창 열기 시 모달 잠금이 수정 전 `false`(해제 안 됨) → 수정 후 `true`(해제됨)로 바뀌었고 로그인 화면 복귀·닫기까지 통과했다. 실제 Google 자격증명은 입력하지 않았다.

남은 확인: 수정 배포의 새 버전으로 iPhone PWA를 다시 열고 Google 이메일·비밀번호 입력 시 OS 키보드가 표시되는지 확인한다. Chromium의 standalone/iPhone 신호와 Auth 에뮬레이터 검사는 실제 iPhone WebKit·키보드 검증이 아니다. 환경변수·Firebase authDomain은 이번 수정에서 변경하지 않는다.


## 2026-09-13 · 프로필·인천 검색 AbortSignal 호환성

- 제보와 원인: 반려견 정보·인천 장소 조회 시 `AbortSignal.any is not a function`. 계정/여행 클라이언트 요청 코드가 미지원 정적 API를 무조건 호출했다. 운영 페이지에서 `any`만 제거하고 검색을 제출해 동일 오류와 서버 검색 요청 0회를 재현했다. 사용자 기기의 OS·브라우저 버전은 확인하지 않았다.
- 수정: `src/lib/http/request-signal.ts`의 AbortController·타이머로 호출자 취소와 계정 30초/여행 290초 제한을 유지한다. 응답 본문 소비를 포함하며 완료·실패 시 타이머와 리스너를 해제한다. 프로필·계정 노트·검색·코스 검사·복구 API에 함께 적용된다.
- 검증: 단위 96개, lint·typecheck·운영 빌드, Firebase 프로필 관련 Playwright 8개 통과. 이 중 데스크톱·모바일의 반려견 등록/저장/재조회 흐름은 `any`와 `timeout`을 모두 제거했다. 수정 전 운영 검사에서 최초에는 자동 추천의 일반 오류 문구를 대상으로 삼아 단언에 실패했으며, 실제 검색 제출 뒤 원래 오류를 확인하도록 검사를 바로잡았다.
- 운영 빌드의 인천 탐색/검색·기존 노트 장소 복원 Playwright 4개도 통과했다. 수정 커밋 `ed07a24`를 main에 푸시한 뒤 운영 `/sw.js`의 새 릴리스와 `/api/health` 정상 응답을 확인했다. 같은 운영 검색 스크립트에서 수정 전 오류/요청 0회 → 수정 후 자동 추천·검색 제출 성공/합성 요청 2회로 바뀌었다. 수정 후에는 `any`·`timeout`을 모두 제거했다. 실제 계정·유료 공급자를 운영 회귀 검사에 사용하지 않았다. 실제 iPhone WebKit/PWA에서의 조회는 사용자 기기 확인이 남아 있다.


## 2026-09-13 · 계정 전용 저장과 상단 노트 UX

- 사용자 요청: 비회원 기기 저장 제거·로그인 유도·새로고침 전 손실 경고, 회원 자동 저장 중심의 제목/노트 목록 배치 개선.
- 비회원: 코스·검사 결과는 현재 Zustand 메모리에만 유지한다. 기기 저장/불러오기/삭제 UI와 구현을 제거하고 이전 기기 기록은 읽거나 이전하지 않는다. 편집/검사 후 beforeunload 기본 확인창을 요청한다. 취소 시 입력/결과 유지, 확인 시 새로고침으로 초기화한다. 최근 검색어는 별도 기기 편의 기능으로 유지한다.
- 회원: 여행 상단의 제목 입력·자동 저장 상태·내 여행 노트·새 여행 노트로 관리한다. 제목/날짜 검색 모달과 열린 노트 표식을 제공하며 정상 전환은 변경사항 저장 후 즉시 연다. 삭제/저장 실패 초안 폐기만 확인한다. 로그인 직전의 입력 또는 검사 결과를 보존하며 로그아웃/다른 계정 전환 시 기존 편집 상태와 note URL 선택을 비운다. 복구 조회에 실패하면 미전송 초안을 유지한다.
- 문서·안내: 제품/디자인/아키텍처/데이터/인증/PWA/테스트 기준과 홈·소개·로그인·오프라인·업데이트 안내를 갱신했다. 오프라인 한국어 문구 변경으로 전용 글꼴을 재생성했다.
- 검증: 단위 95개(폐기한 기기 저장 검사 1개 제거), lint·typecheck·운영 빌드 통과. 오프라인 글꼴의 새 글자 누락을 재생성 후 관련 5개 검사로 해소했다. Firebase 프로필/자동 저장 12개 + 새 UX/계정 분리/충돌 복구 6개 통과. 새 UX 실행기의 첫 종료는 검사 성공 후 143을 반환했지만 복구 검사를 포함한 후속 실행은 정상 종료했다. 운영 브라우저는 54개 통과 후 새로고침 검사 2개의 대화상자 대기 순서를 수정해 2개 모두 재검사 통과했다(개발 전용 2개 제외). /absproxy/3101 basePath의 비회원·검색·계정 준비 검사 8개도 통과했다. 실제 외부 code-server 인증 프록시는 이 검사에 포함하지 않는다. 새로고침 직후 스크린샷의 caret 숨김 스타일이 hydration 전에 삽입되는 검사 간섭은 준비 대기와 caret 유지 옵션으로 분리했다. 변경 커밋 `12ae927`을 main에 푸시했다. 운영 `/sw.js`의 새 릴리스·`/api/health` 정상 응답을 확인했고 운영 노트 UX 스크립트에서 `/plan` HTTP 200·상단 로그인 CTA·기기 저장 제거·새로고침 취소 시 입력 유지·확인 시 초기화·모바일 폭 검사를 통과했다. 검색은 합성 응답이며 실제 계정 쓰기는 0회다. 개발 미리보기를 복원했다.
- 범위: 브라우저가 정한 기본 경고 문구를 사용한다. 사용자 정의 새로고침 모달, 모바일 강제 종료에서 항상 경고, 실제 iPhone PWA 새로고침/키보드 검증을 완료했다고 표현하지 않는다. 실제 계정·유료 공급자 회귀는 실행하지 않는다.


## 2026-09-13 · 운영 프로필·노트 API 500

- 제보: 운영 프로필 GET·개별 노트 PUT 500으로 반려견 이름/계정 노트를 저장하지 못함. 인증 정보 없는 프로필/노트 목록도 빈 본문 500이며 health는 200인 것을 직접 확인했다.
- 재현: Next 배포 추적 파일만 복사한 격리 환경은 Node 24 기본값에서 401, Vercel과 같은 require(ESM) 비활성화 시 ERR_REQUIRE_ESM을 반환했다. Firebase Admin 14.4.0 → jwks-rsa 4.1.0이 ESM 전용 jose 6.2.12를 동기 require하는 초기화 경로가 원인 후보로 확인됐다. 운영 Runtime Logs는 아직 제공받지 않았다.
- 수정: jwks-rsa의 utils/Passport 진입점을 비동기 import로 바꾸는 버전 한정 pnpm 패치. 설치 시 적용되도록 패치·workspace·lockfile을 함께 관리한다. Admin/암호화 SDK 버전과 인증 검증 정책은 유지한다.
- 회귀: require(ESM)을 비활성화한 SDK 로딩·합성 서명 키 검사와 인증 없는 6개 계정 API의 401 JSON 검사를 추가했다. 기존 에뮬레이터/운영 빌드 검증과 푸시·배포 후 결과는 아래에 기록한다.

검증: frozen-lockfile 설치, 단위 96개, lint·typecheck·운영 빌드, demo Firebase Playwright 8개 통과. 수정 후 3개 계정 라우트의 추적 파일만 각각 격리 복사하고 require(ESM)을 비활성화해 6개 메서드 모두 401 JSON을 확인했다. 전체 Next CLI에 같은 플래그를 적용한 첫 E2E 실행은 next.config.ts의 별도 로딩 경로에서 서버 시작 전에 실패했다. Vercel의 빌드/함수 실행 구분에 맞춰 모듈 제한은 위 격리 함수 검사에 적용하고 HTTP E2E는 정상 운영 실행기로 확인한다. 운영 실계정과 유료 API는 회귀에 사용하지 않았다.

운영 빌드 HTTP Playwright 2개(데스크톱·모바일)도 통과했다. 개발 미리보기는 Firebase 검사 후 복원했다.

운영 반영: 수정 커밋 `a6a7ecb`를 main에 푸시했고 GitHub의 Vercel 상태 success, 새 `/sw.js` 릴리스와 health 200을 확인했다. 운영 계정 API 6개 메서드 모두 수정 전 빈 500 → 수정 후 401 UNAUTHORIZED JSON/no-store로 바뀌었다. 가짜 bearer 요청도 Admin 설정 로딩 후 401을 반환했다. 이 변경만으로 운영 초기화 오류가 해소됐지만 실제 사용자 토큰·계정으로 프로필/노트를 쓰지는 않았으며 실제 저장 재시도 결과는 사용자 확인이 남는다.


## 2026-09-13 · 새 로그인 토큰의 운영 401 추적

- 제보: 초기화 장애 수정 후 프로필 GET·노트 PUT이 로그인 만료/401로 실패함. 기존 catch가 모든 verifyIdToken 오류를 만료로 바꿔 원인을 숨겼다.
- 진단: 임시 Firebase 계정의 새 토큰은 로컬 verifyIdToken(checkRevoked=true)에 통과했지만 운영 프로필/노트 목록은 모두 401이었다. Node require(ESM) 비활성화 조건에서도 같았다. 두 진단 계정은 각각 삭제했고 실제 사용자 계정/노트에는 접근하지 않았다. 로컬 Admin과 격리한 배포 SDK의 존재하지 않는 합성 UID 조회는 user-not-found로 정상 도달했다. 운영 클라이언트 프로젝트 ID/API 키는 로컬과 일치했다.
- 조치: 인증 서버의 자격증명/권한/연결/의존성 오류를 503으로 분리하고 고정 reason만 반환/기록한다. 만료/철회/비활성·삭제 계정/잘못된 토큰은 401로 거부한다. 토큰과 SDK 원문 메시지는 기록하지 않는다. 이는 오진 방지이며 운영 저장 문제의 해결 완료를 뜻하지 않는다.

검증: 단위 99개, lint·typecheck·운영 빌드, Firebase 프로필/노트/소유권 Playwright 8개, 운영 빌드 계정 API Playwright 2개 통과. 최초 단위 실행의 TypeScript parameter property 오류는 오류 클래스의 명시적 필드 선언으로 바꿔 해소했다. demo 에뮬레이터 검사 후 개발 미리보기를 복원했다.

운영 진단 결과: `75d7a25`를 main에 푸시한 후 Vercel success와 TOKEN_INVALID 응답 반영을 확인했다. 세 번째 임시 계정으로 발급한 새 토큰은 로컬 철회 검증까지 통과했지만 운영 프로필/노트 목록은 모두 503 / AUTH_CREDENTIAL_INVALID였다. 로그인 만료가 아니라 서버 자격증명 처리 실패임을 확인했다. 진단 계정은 삭제했다. 로컬 서비스 계정의 Auth 및 존재하지 않는 합성 문서에 대한 Firestore 읽기도 통과했다.

남은 조치: Vercel 환경변수 조회/수정 연결이 없어 운영 설정은 아직 바꾸지 못했다. 검증된 로컬 FIREBASE_PROJECT_ID·FIREBASE_CLIENT_EMAIL·FIREBASE_PRIVATE_KEY만 담은 `.env.vercel.firebase.local`을 권한 0600으로 만들고 Git 제외 및 파싱 왕복을 확인했다. 사용자가 Vercel Production의 해당 3개 값을 이 파일로 다시 적용한 후 Redeploy해야 한다. 파일/키 내용은 커밋·로그·채팅에 기록하지 않는다. 운영 저장 성공은 아직 확인되지 않았다.

## 2026-09-16 · 경기 북서부 선정과 공식 음식점 안내

- 수요·여행지역 응답·공급 데이터 비교를 통해 고양·파주·양주를 기본 탐색 및 후속 검수 지역으로 선정했다. [조사·구현 범위](../data/REGION_SELECTION_2026-09-16.md)에 통계 연도·분모, 후보 비교, KTO 표본 한계와 공식 목록 활용 조건을 기록했다. 기존 인천 검색·검수 자료는 유지한다.
- 경기 북서부·도시 바로 찾기, 경기/강원/인천 지역 검색, 카테고리 변경 시 지역 유지, 콘텐츠 ID 중복 제거를 구현했다. 같은 주소의 다른 콘텐츠 ID는 강제 병합하지 않는다.
- 코스에 담은 실제 식당·카페의 상호·주소를 식약처 사이트와 대조하는 안내 및 2026년 개정 매뉴얼/FAQ의 방문 준비 안내를 연결했다. 목록 자동 병합·상세 규정 자동 보완은 미구현이며 일반 안내로 판정을 승격하지 않는다.
- 검증: 단위 101개, lint·typecheck·운영 프로필 빌드 통과. 관련 Playwright 데스크톱/모바일 6개 통과. 최초 E2E 실행은 기존 개발 서버 락으로 시작하지 못했으며 관리 미리보기를 종료한 뒤 통과했다. 실제 KTO 어댑터 검색 4회로 23항목·음식점 6항목을 확인했다. 음식점 상세 규정 완전성·유효 대체 코스·실기기 검증은 완료하지 않았다.
- 운영 확인 도구: `node scripts/check-pilot-region.mjs https://pawproof-rose.vercel.app --live`. 읽기 전용 검색 2번(서버 KTO 최대 5회), 합성 응답 없이 실제 검색·카페 추가·공식 안내와 모바일 폭을 점검한다. LLM 호출·계정 저장·공식 사이트 메시지 전송은 하지 않는다.

운영 반영: 기능 커밋 `0a6e8c8`을 `main`에 푸시한 뒤 운영 `/plan`에서 새 기본 지역 문구와 HTTP 200, `/api/health`의 200/status=ok를 확인했다. `scripts/check-pilot-region.mjs`가 실제 운영 API 검색 2번으로 집중 권역 23항목·카페 3항목을 수신하고 카페 추가→공식 목록 대조 안내를 여는 흐름을 통과했다. Chromium 390×844에서 가로 넘침이 없으며 화면은 Git 제외 `.cache/regions/production-mobile.png`로 확인했다. 실제 휴대폰·Google 로그인·계정 저장·LLM 규정 판정은 이번 배포 점검 범위에 포함하지 않았다. 개발 미리보기는 재시작했다.

## 2026-09-16 · 지도 중심 탐색

- 1단계 `660a347`: 좌표·반경 검색, 최대 5곳 규정 조회, 입력 계약·호출 상한, 누락을 확인 필요로 유지하는 조건 평가와 문의 문구. 관련 단위 9개·lint·typecheck·운영 빌드 통과.
- 2/3단계: `/plan` 실제 모드를 지도로 전환하고, `view=note` 및 가상 체험은 기존 노트로 진입한다. 내 위치·여행지 선택·유형·반경·실내외·반려견 프로필을 지원한다. 선택된 장소 또는 가까운 5곳만 버튼으로 규정을 조회하며 지도 이동·필터 변경은 LLM 호출을 발생시키지 않는다.
- 운영 키 `KAKAO_MAP_KEY`와 로컬 `NEXT_PUBLIC_KAKAO_MAP_KEY`를 공개 JavaScript 키로 연결한다. 사용자 추가 도메인 `pawproof.kr`과 기존 Vercel 도메인은 별도 검증한다. REST/서버 자격증명은 공개 빌드 변수에 포함하지 않는다.
- 지도와 노트는 동일 PlannerProvider를 공유한다. 확인한 규정·GPS는 활성 화면 메모리 전용이며 계정 노트에는 기존 허용 필드만 저장한다. 카카오 장소 상세 ID를 확보하지 않은 링크는 상호·주소 검색으로 명시한다.
- 검증: 단위 106개, lint·typecheck, 기존 관련 브라우저 16개 및 지도 브라우저 4개 통과. 최초 SDK 모형의 닫는 괄호 오류를 수정하고 지도 4개를 재실행했다. 모형 검사는 카카오 운영 승인·실제 타일이나 실기기 검증이 아니다.

## 2026-09-17 · 모바일 지도 중심 UX와 조건 안내

- 모바일 지도는 검색·필터를 상단 고정 영역 대신 지도 위 버튼과 하단 drawer로 관리한다. 지도 높이를 화면 중심으로 확장하고, 장소 상세를 닫은 뒤에도 지도에서 다른 장소를 계속 담을 수 있게 했다. 기존 장소 유형·구역·반경 필터는 서비스 공통 Select를 사용한다.
- 조건 필터 중 ‘우리 조건과 불일치하는 곳 제외’와 ‘조건이 확인된 곳만 보기’는 제거했다. 겹친 지도 숫자는 해당 위치에 겹쳐 표시된 후보 장소 개수이며, 숫자를 누르면 모든 장소를 펼쳐 각각 코스에 담는다.
- 프로필 반려견은 현재 입력을 덮어쓰지 않고 등록 반려견을 토글로 추가한다. 동일 반려견은 중복 추가하지 않으며 선택된 칩은 진한 테두리로 표시한다. 체중 미확인 문구는 업체 제한 정보 부재를 확인하도록 바꾸고, 예방접종 증빙 규정은 별도 규칙·판정·상세 안내로 보존한다.
- 문의 문구는 API 재호출 없이 출처 판정 결과를 사람이 바로 보낼 수 있는 인사·방문 정보·질문 목록으로 조립한다. OpenRouter는 규정 추출에만 사용해 문의 복사 동작의 지연·비결정성을 만들지 않는다.
- 초기 지도 중심은 현재 파일럿 후보 수가 가장 많은 파주로 옮겼다. 이는 반경별 실시간 최고 밀도 계산이 아닌 파일럿 데이터 기준이며, 후보 수와 출처를 화면에서 함께 고지한다.

검증: 단위 107개 통과, 직접 `tsc --noEmit`·관련 ESLint 통과. 실제 개발 서버의 데스크톱 지도 화면에서 파주 초기 중심·지도 경로·검색/필터 영역을 확인했다. 모바일 실기기와 카카오 운영 타일에서의 터치 조작·키보드 확대는 아직 확인하지 않았다.

## 2026-09-18 · 장소 선택 자동 조회와 사용자용 근거 문구

- 장소 규정 조회는 일괄 조회 버튼을 제거하고, 사용자가 장소를 선택한 순간 해당 장소 1곳만 자동 조회하도록 바꿨다. 선택 상세에 조회 중 상태를 표시하고, 실패하면 해당 장소만 다시 조회할 수 있다.
- KTO 원문과 규정 검증에는 내부 필드명을 유지하되, 장소 상세·근거 대화상자·준비표·원문 보기에는 `etcAcmpyInfo:`·`usetim:` 같은 접두사를 숨겨 실제 행동과 안내만 보이게 했다.
- 모바일 지도 도구는 검색 버튼 하나가 검색·필터 drawer를 열도록 정리했다. 여행 노트의 모바일 코스 검사 바는 하단 내비게이션 높이에 맞춰 내려 배치했다.

검증: 단위 108개, 직접 `tsc --noEmit`·관련 ESLint 통과. 390×844 브라우저 점검에서 검색 버튼 1개·drawer 열림·검사 바 `bottom: 72px`를 확인했다. 실제 KTO 규정 추출과 실기기 터치 검증은 포함하지 않았다.

운영 프로필 빌드 통과. 첫 화면 빌드는 라우트 생성까지 완료 후 종료 코드 143을 반환하여 성공으로 집계하지 않았고, 재실행에서 종료 코드 0을 확인했다.

운영 점검: `660a347`, `5524d52`를 main에 푸시했다. 두 도메인의 `/plan` 새 화면·health 200 및 주변 검색 200을 확인했다. 초기 중심 5km 후보는 1건이었으며 전체 지역 공급 건수로 해석하지 않는다. 실제 지도 smoke는 두 도메인 모두 SDK 로딩 실패로 통과하지 못했다. `pawproof.kr`의 SDK 요청을 별도 확인한 결과 HTTP 403, `NotAuthorizedError: App(pawproof) disabled OPEN_MAP_AND_LOCAL service.`가 반환됐다. 키에 연결된 카카오 앱의 지도·로컬 서비스 활성화가 필요하다. 키 값은 로그·문서에 기록하지 않았다. 사용 설정 후 실제 타일·마커 및 지도→노트 흐름 재검증이 남아 있다. 이를 코드 테스트나 배포 성공으로 대체하지 않는다.

지도 운영 재검증: 사용자 키 교체 직후에는 로컬 키의 두 origin SDK 응답만 200이고 운영에는 이전 키가 남아 있었다. 재배포 완료 안내 후 `scripts/check-map-discovery.mjs`를 두 도메인에 실행해 모두 종료 코드 0을 확인했다. 실제 SDK·타일·마커, KTO 주변 후보 1건, 카카오 상호·주소 검색 링크, 장소 담기→노트→지도 복귀 상태 유지 및 가로 넘침 검사를 통과했다. 각 호스트 주변 조회 2회, LLM 호출 0회. `.cache/maps/pawproof.kr.png` 화면도 확인했다. 모바일 Chromium 에뮬레이션 결과이며 실기기·실계정 저장·실제 GPS·규정 추출 검증은 포함하지 않는다. 키·월렛·카카오 앱 설정은 이 작업에서 수정하지 않았다.

## 2026-09-17 · localhost 개발과 모바일 지도 편집 개선

- 헤더 소개 링크 제거(`b6294f2`). 지도 재검색 버튼의 가로 정렬을 hover transform에서 분리하고 반경을 공통 Radix Select로 교체했다. 지도 아래 코스 요약·출처·여백을 추가했다.
- 모바일 큰 지도·하단 지도/노트 내비게이션·비모달 drawer를 구현했다. 담기 후 drawer를 닫고 지도에 머물며 완료로 동일 노트를 연다. 계정 자동 저장과 비회원 메모리 계약은 유지한다.
- 기존 숫자는 목록 순번이었다. 개별 장소 이름 / 겹친 후보 `N곳` / 담은 `코스 N`을 구분했다. 묶음 전체 조회·확대 재분리·동일 좌표 전체 접근·검색 변경 후 담은 장소 유지, 순서 점선·전체 보기·노트의 순서 변경/삭제 반영을 검증했다. 점선은 도로 길찾기가 아니다.
- 검증: 단위 106개, lint·typecheck, 운영 빌드 통과. 직접 개발에서 지도/기존 코스/비회원/상태 20개, 최종 지도/공통 컨트롤/환경 20개(운영 전용 2개 제외), 프록시 지도 8개, 운영 빌드 지도/환경 14개(개발 전용 2개 제외) Playwright 통과. 중복 실행한 지도 사례를 별도 새 테스트 개수로 합산하지 않는다.
- 새 MacBook의 Playwright Chromium을 설치했다. 최초 브라우저 미설치 실패와 이후 모형 마커의 버튼 겹침 실패는 환경·fixture를 수정하여 해소했다. 실제 지도와 동일한 좌표 계산을 모사하지만 실제 타일 검증과 구분한다.
- Chrome localhost에서 실제 SDK·타일·장소 선택·drawer와 390/320px 폭을 확인했다. 초기 SDK 로딩 실패는 서버 재시작 후 재현되지 않았으며 키/허용 도메인을 변경하지 않았다. SDK 원응답도 200이었다. 실기기 터치·iPhone 키보드·실계정 저장은 이번 회귀 검증 범위 밖이다.
- 개발 서버는 직접 로컬 프로필로 실행하고 `.cache/localhost-dev.log`, `.cache/localhost-dev.pid`에 분리했다. 프로세스 종료 전에 PID의 명령과 경로를 확인한다.

localhost 실제 smoke(`scripts/check-map-discovery.mjs http://localhost:3000 --live`)도 통과했다. 실제 후보 1건·SDK/타일·담기→완료→노트→지도 복귀와 선택 유지, 가로 넘침 없음을 확인했다. 화면 전환의 주변 재조회까지 포함하여 도구 상한은 3회로 조정했고 LLM은 차단했다. 기존 2회 상한에서는 세 번째 주변 재조회가 의도적으로 차단되어 실패 안내가 표시되었으므로 상한 수정 후 재확인했다.
