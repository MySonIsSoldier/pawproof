# 의사결정 및 이전 대화 히스토리

정리일: 2026-09-10\
입력 자료: 사용자가 붙여 준 과거 ChatGPT 대화, 최초 요청의 공모전 안내·제출 아이디어, 2026-09-10 기획 제안과 사용자 승인.

이 문서는 대화 전문의 보관본이 아니라 **결정과 근거의 요약**이다. 대화에 포함된 API 인증키는 의도적으로 제외했다.

## 1. 공모전 참가와 주제 탐색

사용자는 생활관 선임과 2인 팀으로 웹·앱 구현 부문에 참가하고, 10개 지정과제 중 독창적이며 실제 구현 가능한 아이디어를 탐색했다. 초기 요청에는 전년도 수상작과 대회 관계자 발언을 분석해 주제를 추천해 달라는 의도가 있었다.

현재 상태는 사용자 설명상 예비 아이디어 제출을 통과했으며 제품 구현을 준비하는 단계다. 당시 공고 PDF·검증 키트·결과 파일은 현재 작업 공간에서 확보하지 못했다.

## 2. PawProof 초기 방향

이전 대화는 PawProof를 여행 코스 전체의 성립 여부를 검증하고 실패 지점만 복구하는 서비스로 제안했다.

초기 핵심 조합:

- 3~5개 방문지의 규정 동시 검사.
- 코스에서 문제가 되는 장소 특정.
- 같은 유형의 장소로 교체.
- 교체 전후 추가 이동시간 표시.
- 판정 원문·출처·시점 표시.
- 실제 규정에서 준비물 생성.

당시 제외 제안: 일반 지도 중심 서비스, 숙소 예약, 커뮤니티, 예방접종 PASS, AI 여행 챗봇, 자동 전화.

## 3. 데이터 후보와 시연 지역

이전 대화는 KTO 전국 데이터에 인천 집중 시연 지역을 결합하고, 인천 CSV·식품안전나라·문화정보원·Kakao Local·Kakao Directions를 보완 데이터로 제안했다.

스키마·행 수 확인과 실제 원본 품질 검증은 서로 다른 상태였다. 현재 이 기록은 [데이터 전략](../data/DATA_STRATEGY.md)에 구분해 보존한다. 인천을 이미 고신뢰 지역으로 검증했다고 간주하지 않는다.

## 4. 서비스명 결정

이전 대화는 해외의 유사 이름 앱을 이유로 PawProof를 코드명으로 두고 ‘반려완주’를 제출명으로 제안했다.

**사용자는 PawProof 이름을 그대로 사용하기로 명시적으로 결정했다.** 현재 서비스명은 PawProof(포프루프)이며 이름 변경을 전제로 기획하지 않는다. 이 결정은 별도의 상표·권리 검토 완료를 뜻하지 않는다.

## 5. 과거 API 검증 요청과 보고

사용자는 KTO 개발계정 승인을 받은 뒤 인증키를 제공하고 직접 데이터 검증을 요청했다.

이전 답변은 91회 호출·오류 0건, 최신 수정순 30개 장소, 체중 2건·마릿수 1건 등 결과를 보고했다. 키를 명령어·로그·결과 파일에 저장하지 않았다고도 설명했다.

현재 문서화 과정에서는 이를 **과거 대화의 보고**로 취급한다. 원응답·검증 리포트를 확보하거나 호출을 재현하지 않았으며, 당시 보관 방식까지 독립적으로 확인한 것은 아니다.

현재 구현에 가져올 판단:

- 규정 추출→규칙 판정→근거 표시 구조를 유지한다.
- 누락은 제한 없음으로 해석하지 않는다.
- 반복 필드와 문맥을 함께 검토한다.
- 전화번호와 숙박 규정은 별도 검증이 필요하다.

## 6. 제출 개요의 표현 조정

사용자는 처음에 기술 용어를 더 포함하기를 원했다. 이후 기술 설명이 과해 서비스의 의미가 흐려진다고 지적했고, 사용자 효용과 기술을 균형 있게 표현하도록 수정했다.

대화에 명시된 7월 17일 오후 7:56 요청 이후 정리된 제출 개요:

> 한국관광공사 반려동물 동반여행 OpenAPI의 장소별 출입 조건을 LLM 기반 정보추출로 구조화하고, 반려동물의 견종·체중·마릿수와 여행 일정에 대조해 각 방문지를 이용 가능·준비 필요·확인 필요·이용 불가로 근거와 함께 판정하는 여행 코스 사전검증 웹앱입니다. 조건에 맞지 않는 장소는 규칙 엔진과 동선 최적화를 통해 같은 유형의 대체 장소로 추천하여 현장 입장 거부와 일정 차질을 줄입니다.

최초 요청에서는 ‘카카오맵 등 업체의 공개 정보’를 구조화 대상으로 포함한 표현도 제시됐다. 현재 기획에서는 공개 화면과 실제 API 제공 범위를 구분하고, 확보·이용 가능한 공식 안내부터 보완한다.

## 7. 2026-09-10 기획 보강과 사용자 승인

사용자는 대회에서 1등을 목표로 구현하기 전에 기획을 단단히 해 달라고 요청했다. 공유 링크 본문을 직접 읽지 못하자 이전 대화 내용을 텍스트로 제공했다.

기획 제안은 다음을 보강했다.

1. 지정과제 6번의 문제를 공식 목록에서 확인.
2. ‘원래 여행을 최대한 유지하면서 해결한다’를 중심 가치로 설정.
3. 대체 장소 전에 같은 장소의 준비·구역·시간 변경 가능성 검토.
4. 당일 자가용 여행 3~5곳으로 제출 범위 집중.
5. 네 상태의 의미와 미확인 정보 처리 명확화.
6. 대체 후 이후 일정 전체 재검증.
7. 최종 코스·준비사항·확인 질문을 하나의 결과로 제공.
8. 인천은 실제 대체 후보 밀도를 검증한 후 확정.
9. 오판과 판정 가능 범위를 함께 평가.
10. 09.20 제출 목표와 2인 역할 분담 제안.

사용자는 해당 기획을 승인하고 `docs` 폴더에 현재 기획·대회 개요·히스토리를 마크다운으로 정리하도록 요청했다. 현재 문서들은 이 요청의 산출물이다.

## 8. 기존 제안에서 수정하거나 한정한 사항

| 과거 표현·가정 | 현재 기준 |
|---|---|
| PWA 설치까지 구현하면 앱도 선택 가능 | 현재 공지는 앱 스토어 링크 요구. 웹으로 제출 |
| 인천 고신뢰 시연 지역 | 우선 후보. 원본·조건·대체 밀도 검증 후 확정 |
| 전국 장소·숙박까지 동일 흐름 | 제출 우선 범위는 반려견 자가용 당일 여행. 숙박 별도 검증 |
| API 성공 및 최신 30개 표본으로 데이터 적합성 판단 | 연결 가능성의 근거. 전국 커버리지·신선도 증명은 아님 |
| 단순한 장소 단위 허용 여부 | 선택한 구역·서비스·시간·일행 기준 판정 |
| 문제가 있으면 장소 교체 | 원문에 근거가 있으면 준비·구역·시간 변경도 검토 |
| 가까운 같은 업종으로 대체 | 목적·조건·고정 일정 충족 후 시간 변화 비교 |
| API 정보를 DB에 보관해 재사용 | 공모전 실시간 호출 권고 반영. 원문·파생 규정 저장 범위 확인 |
| 카카오 공개 정보로 규정 확보 | Local 공식 응답과 웹 화면 내용 구분. 확보 경로 검증 |
| 오늘 조회했으므로 최신 규정 | 조회 시각·콘텐츠 수정일·규정 확인일 구분 |
| 통과·조건부·불가 등 혼용 | 이용 가능·준비 필요·확인 필요·이용 불가로 통일 |
| 해외 유사 이름 때문에 반려완주 권장 | 사용자 결정에 따라 PawProof 유지 |

## 9. 아직 확정하지 않은 사항

- 실제 접수된 지정과제 값과 최종 팀 정보.
- 공식 공지 URL, OT 자료, 제출 양식·매뉴얼.
- 현재 API 응답·데이터 품질과 과거 결과의 재현 여부.
- 인천 집중 권역, 최종 장소 목록, 실제 대체 후보.
- KTO 원문·파생 규정의 보관·캐시 허용 범위.
- 채택한 기술 스택의 실제 배포 적합성, 지도·이동시간 API 설정과 비용.
- OpenRouter에서 사용할 모델과 실제 사용량 예산·연결 검증.
- 최종 도메인 선택과 사용자 제공 디자인 레퍼런스.
- 현재 개발자인 사용자의 실제 가용시간 및 외부 검수·사용자 테스트 모집.
- 구현·정확도·사용자 효과의 실측 결과.

상세 구현을 진행하면서 확정한 결과는 관련 기준 문서와 이 기록에 함께 반영한다.

## 10. 2026-09-10 개발·운영 기획

사용자는 무료 호스팅을 우선하고 Vercel·Firebase·Next.js를 제시했다. 보유 도메인은 Cloudflare DNS에서 관리하는 `hothyun.com`이며, 새 도메인 구매는 직접 결정하고 디자인 레퍼런스도 직접 제공하기로 했다.

현재 개발 환경은 사용자 설명상 OCI의 Coolify에서 호스팅한 code-server다. 프록시를 통한 개발과 루트 경로 배포를 환경변수·공통 유틸로 분리하고, 클린 코드·클린 아키텍처·단일 책임 원칙을 따르도록 요청했다.

후속 확인에서 GitHub **Organization의 공개 저장소**를 사용한다고 밝혔다. Vercel이 무료로 맞지 않으면 Cloudflare Workers도 허용 가능한 대안으로 제시했다.

이번 공식 문서 검토에 따른 권고:

1. 공개 Org 저장소는 비공개 Org에 적용되는 Vercel Hobby 제한과 구분한다. Next.js + Vercel Hobby + Firestore Standard/Spark를 우선 검증한다.
2. Hobby의 개인·비상업 용도 조건과 실제 배포 연동은 별도 확인한다. 무료 대시보드 팀 권한이 GitHub 협업과 같다고 가정하지 않는다.
3. Firestore만 DB로 사용하고 Firebase Functions·Storage·App Hosting의 결제 계정 요구를 혼동하지 않는다.
4. Supabase의 비활동 일시정지와 Workers Free의 요청당 CPU 제한을 비교에 반영한다.
5. Cloudflare로 전환하면 Next.js + OpenNext + D1을 검증하되, 현재 Cloudflare 신규 앱 안내의 vinext 베타 권고와 구분한다.
6. `/proxy`는 접두사를 제거하므로 `/absproxy`와 Next `basePath`를 맞추는 개발안을 권고한다. 배포는 루트 경로로 별도 빌드한다.
7. 규칙 도메인과 사용 사례를 외부 SDK·호스팅·DB에서 분리한다.
8. 기존 기획 문서를 목적별 폴더로 옮기고 개발·운영 문서를 추가한다.

LLM 비용 범위 질문은 아직 답변받지 않았다. 문서 작성은 진행하되 유료 공급자 사용을 승인된 것으로 취급하지 않았다. 이 기록은 기획 결과이며 실제 코드 구현·API 재검증·DNS 변경·배포를 의미하지 않는다.

## 11. 2026-09-10 기본 스택 채택과 1인 개발 규칙

사용자가 우선안으로 진행하기로 결정했다. 기본 스택은 Next.js App Router + TypeScript + pnpm + Vercel Hobby + Firestore Standard/Spark이며 실제 배포·성능 검증은 별도다.

현재 코드 작업자는 사용자 1명이고 `main`에서 계속 작업한다. 기능을 개발할 때마다 검증 후 커밋을 남기고, 독립적으로 검토·되돌릴 수 있는 작은 단위로 나누도록 요청했다. 기능 브랜치나 PR을 필수로 하지 않는다. 과거 2인 대회 참가 기록은 현재 개발 인원과 구분해 보존한다.

클린 코드·클린 아키텍처 기반의 단일 책임, 안쪽을 향하는 의존성, 순수 도메인, 얇은 UI·라우트, 외부 입력 검증을 상세 코드 규칙으로 보강했다. 루트 `AGENTS.md`에는 핵심 규칙과 `docs/` 탐색 경로만 간략히 정리한다.

## 12. 2026-09-10 초기 구현 착수

사용자는 보유 OpenRouter 크레딧을 LLM API 호출에 사용하는 방향을 제시했다. 배포·DB 연결 준비는 직접 담당하고 디자인 레퍼런스는 추후 제공한다. KTO 반려견 API 사용 요청도 남겼다고 설명했다. 이는 현재 계정의 승인·실응답을 확인한 결과와 구분한다.

첫 구현 범위는 Next.js 초기화, 환경변수 검증, code-server·배포 경로 유틸이다. 서비스 본 기능·외부 API 호출·DB 연결·외부 배포는 이번 범위에 포함하지 않는다. Next.js 16.3.4, React 19.3.0, TypeScript 5.9.3, pnpm 12.3.4를 고정하고 최소 시작 화면·소개·상태 확인 API로 기반을 구성한다.

초기화와 환경·경로 구현을 별도 커밋 단위로 나눴다. 키 없는 빌드, 순수 함수 테스트, 실제 code-server 바이너리의 로컬 프록시 경유 브라우저 검증을 수행했다. 현재 작업 공간에는 자격증명 없는 `.env.development.local`을 준비했다. 최종 검증 범위는 [구현 기록](../delivery/IMPLEMENTATION_STATUS.md)에 남긴다.

## 13. 2026-09-11 개발 미리보기 접속 복구

사용자의 접속 장애 요청에 따라 3000번 개발 서버의 응답 지연과 `write EPIPE` 로그를 확인했다. 기존 도구의 출력 파이프에 연결된 앱을 종료하고, 별도 세션에서 파일 로그를 남기도록 재실행했다. 의존성 설치 전 서버 중지·설치 후 재시작과 인증/앱 장애 구분 절차를 개발 환경 문서에 추가했다.

복구된 서버에서 기존 브라우저 검사 2개가 통과했다. Cloudflare Access의 미인증 리디렉션은 확인했지만 외부 로그인 세션까지 검증한 것은 아니다. 앱 코드나 프록시 계약을 바꾸지 않았으며 상세 근거는 [구현 기록](../delivery/IMPLEMENTATION_STATUS.md)에 남긴다.

## 14. 2026-09-11 제품 구현과 디자인 위임

사용자가 후속 기획·구현 판단을 위임하고 ref/ 레퍼런스, 한국어 글꼴 선택, DESIGN.md 선작성, 인터랙티브 UI와 Playwright 자동화·기능별 커밋을 요청했다. DESIGN.md와 실행 계획을 먼저 작성했다. 키 없는 가상 체험과 실제 API 모드를 명시적으로 구분하며, 외부 설정값 안내는 구현 후 제공한다.

## 15. 2026-09-11 제품 화면과 서버 흐름 구현

숲색·세이지와 자체 호스팅 Pretendard, 곡선 노선도와 반려견 사진을 사용했다. 가상 장소 7곳의 체험과 실제 API 모드는 명확히 분리한다. 프로필·코스 편집 → 네 상태 판정 → 원문 근거 → 대체/되돌리기 → 준비표/문의 문구를 구현했다. 저장은 명시적으로 선택한 브라우저 사용자 입력에 한정한다. 서버 규정 캐시·Firestore 연결은 구현하지 않는다. 지도 SDK 대신 순서 노선도를 제공하고 실제 도로 시간은 Kakao 서버 어댑터로 조회한다.

Playwright를 데스크톱·모바일·직접/프록시/운영 모드로 실행하는 스크립트와 미리보기 수명 관리 스크립트를 마련했다. 자동 테스트에서 찾은 개발 모드 dialog 닫힘을 수정했다. 테스트·실측 결과와 남은 API 설정 조건은 구현 기록과 API_INTEGRATION.md에 구분한다.

## 16. 2026-09-11 인쇄와 한국어 화면 검증 보완

출발 준비표를 인쇄할 때 검사 대상 날짜·반려견·첫 방문 시각을 남기고 모바일 검사 버튼을 숨긴다. 근거 원문 글자 크기를 보완했다. Playwright의 한국어·서울 시간 환경에서 화면/인쇄 전환을 검사하고 직접 개발·프록시·운영 환경 전체 검증을 통과했다.

## 17. 2026-09-11 반복 검사와 미리보기 실행 표준화

`pnpm verify`를 전체 검사의 진입점으로 정하고 관리 중인 미리보기만 중지·복원한다. `preview:start/stop`은 Linux PID의 경로와 명령을 확인하며 로그를 파일로 분리한다. 개발 프로필 전환 후 HMR 갱신 실패를 발견해 개발 컴파일 디스크 캐시를 비활성화했다. 전체 검증과 추가 HMR 4회 반복 통과를 확인했으며 운영 빌드 캐시 설정은 유지한다.

## 18. 2026-09-11 RHWP 확장 프로그램의 hydration 경고 진단

사용자가 RHWP 설치 상태와 html 속성 불일치 경고를 보고했다. 홈·가상 코스의 정상 브라우저와 동일 속성 주입 브라우저를 비교해 주입 시에만 경고가 발생함을 확인했다. 앱 렌더링 코드는 유지하고 확장 프로그램 비활성화 또는 사이트 접근 제한을 안내한다. 재사용 진단 스크립트와 실행 결과는 TESTING.md에 기록했다.

## 19. 2026-09-11 shadcn 입력과 디자인 시스템

사용자 요청에 따라 native 날짜·시간·선택 팝업을 shadcn 기반 Radix Select/Popover와 한국어 Calendar로 교체한다. 브랜드 색상과 초록 포커스 링을 공통 토큰으로 관리하며, 1분 단위 시각·기존 저장 체류시간을 보존한다. 입력 컴포넌트는 src/components/ui로 분리하고 공식 소스의 MIT 고지를 유지했다. 데스크톱·모바일 입력 흐름 4개, 타입·린트·빌드 검증 후 커밋한다.

추가 요청으로 전체 UI 컴포넌트 규칙과 상태 책임을 점검한다. 후속 리팩터링에서는 Zustand의 화면별 편집 상태와 TanStack Query의 요청 생명주기를 분리하고, SSR 초기값 일치·명시적 저장·유료 자동 재호출 방지 기준을 유지한다.

## 20. 2026-09-11 상태·요청·공통 UI 리팩터링

Planner의 요청·저장·화면 상태를 Zustand store, TanStack Query hooks, 저장 어댑터, 표시 패널로 분리했다. 서버/클라이언트 초기 날짜는 동일 props로 전달하고 도메인의 현재 시각은 인자로 주입한다. 교체 후 추가 편집을 하면 되돌리기를 무효화해 새 편집을 지우지 않으며, 이전 revision의 응답/후보는 채택하지 않는다.

shadcn 기반 Button/Checkbox/Dialog와 개발용 디자인 시스템 갤러리를 추가하고 근거 모달을 Radix로 이전했다. 공통 UI·domain·application 경계를 ESLint로 검사한다. 자동 재시도·포커스 재조회·디스크 캐시는 사용하지 않고 명시적 입력 저장만 유지한다. 상세 계약은 UI_DESIGN_SYSTEM.md, 실제 검증 범위는 IMPLEMENTATION_STATUS.md를 따른다.

## 21. 2026-09-11 코드 품질 점검과 Sonner 작업 알림

사용자 요청에 따라 Sonner 2.0.8을 공통 UI에 추가하고 `withNotifications` HOC를 Planner와 개발 갤러리에 재사용했다. 결과 이벤트와 표시를 분리하고 반복 작업도 다시 안내하되 최신 알림 하나만 유지한다. 상세 결과·오류는 화면에 남기며, 입력 타이핑에는 알림을 띄우지 않는다. 취소된 요청과 화면 이탈 후 비동기 완료의 알림을 막고 복사 실패를 오류로 구분했다. 프록시 HTML 응답의 JSON 파싱 오류는 한국어 재시도 안내로 변환한다. 단위·브라우저 검사 범위와 실제 결과는 IMPLEMENTATION_STATUS.md에 기록한다.

## 22. 2026-09-11 PWA 우선 구현

사용자는 PWA 도입을 요청한 뒤 실제 API 키 검증과 Firebase·인증은 이후 단계로 명확히 구분했다. 먼저 설치 가능한 매니페스트·아이콘, 설치 안내, 정적 오프라인 fallback, 사용자 선택형 워커 업데이트를 구현한다. 원문·결과·사용자 입력을 워커 캐시에 넣지 않고, 개발 프록시와 운영의 scope를 분리한다. 사용자 입력을 지키기 위해 강제 새로고침과 자동 API 재시도를 사용하지 않는다. 브라우저 자동검사와 실제 OS 설치 확인을 구분한다.

## 23. 2026-09-11 실 API 연결·브라우저 검증

사용자가 KTO·OpenRouter·Kakao 키 입력을 완료하고 실사용 검증을 요청했다. 실제 API 호출에서 Gemini 스키마 복잡도 거부, 제공 물품/연령 예외 해석, 지원 업종 검색 누락, 실정책 응답의 불필요한 place 필드로 인한 클라이언트 검증 실패를 발견해 수정했다. 생성용 스키마를 단순화하면서 서버의 길이·수치·근거 검증은 유지했다. 실제 3개 장소의 검색·판정·근거·대체 탐색·프로필 재검사·입력 저장/불러오기가 통과했다. 표본은 모두 확인 필요였으며 유효 대체 0건을 실제 교체 성공으로 기록하지 않는다. 재실행은 engineering/LIVE_VALIDATION.md, 수치·비용·한계는 delivery/LIVE_VALIDATION_2026-09-11.md에 정리한다. 원문/키 저장·Firebase·인증·배포는 추가하지 않았다.

## 24. 2026-09-11 인천 데이터 완전성 보강

사용자가 인천의 데이터 완전성 보강을 요청했다. 인천을 집중 검수 지역으로 확정하고 시 CSV 162행의 원본·이용허락·채움률을 확인했다. 9곳의 ID·이름·도로명 번호를 대조해 보완 자료를 연결하고 다른 주소·이용 범위는 병합을 보류했다. 인천 검색은 현재 법정동 코드로 확장해 43곳을 반환하며 신규 업종 분류로 카페 누락도 수정했다. 출처별 추출·구역별 충돌 처리·근거와 연락처 표시, 날짜가 명시된 송도 도그파크 9/12 휴장을 구현했다. 공개 자료의 마릿수·복잡한 시간 조건은 여전히 부족하므로 전체 완전성을 달성했다고 표시하지 않는다. 검증 내용은 delivery/INCHEON_VALIDATION_2026-09-11.md, 후속 질문은 data/INCHEON_CONFIRMATION_QUEUE.md에 기록했다. 외부 문의 발송·Firebase·인증·배포는 하지 않았다.

## 25. 2026-09-12 군 복무 여건에 맞춘 앱 자료 조사

사용자는 직접 방문이 어렵고 타 플랫폼의 앱 정보를 확인할 수 있다고 설명하며 구체적인 작업 문서를 요청했다. 방문·전화 대신 반려생활·네이버지도 중심의 원격 조사로 사용자 역할을 정했다. 누닝·개떼놀이터 인천점·문차이나·뻘다방 4곳을 우선하고 장소별 주소·검색어·누락 조건·캡처·전달 양식·조사 종료 기준을 data/INCHEON_APP_RESEARCH_GUIDE.md에 작성했다. 공개 웹·시설 공지는 Codex가 맡는다.

개별 업소의 앱 등록과 최신 메뉴는 미확인으로 표시한다. 앱 열람·업체 확인·현장 검증을 구분하고, 같은 원천의 재게시를 독립 확인으로 계산하지 않는다. 이번 변경은 조사 안내 문서이며 신규 규정 확보·제품 반영·외부 연락을 수행한 것은 아니다.
