# PawProof 디자인 기준

2026-09-11 · 시각 디자인과 인터랙션의 기준. 제품 정책은 docs/product/, 구현 기록은 docs/delivery/를 따른다.

## 레퍼런스와 해석

사용자 제공 `ref/ref1.webp`~`ref4.webp`를 직접 확인했다. ref1의 세이지 바탕·짙은 녹색 제목·널찍한 여백, ref2의 모바일 흐름, ref3의 곡선 연결과 친근한 반려견, ref4의 색상·타입 체계를 참고한다. 원본의 로고·사진·실적·리뷰는 제품에 복사하지 않는다. 참고 이미지는 사용자 자료이며 이번 커밋에 포함하지 않는다.

방향: **숲빛 여행 노트**. 출발 전 여행 노트를 펼쳐 우리 강아지의 조건과 코스를 차분히 확인하는 경험. 신뢰를 주는 정보 밀도와 가볍고 따뜻한 여행 감성을 함께 만든다.

## 색과 형태

- 주색 forest `#2f6b50`, 깊은 잉크 `#193d30`, 세이지 `#eaf0e9`.
- 페이지 바탕 ivory `#fafbf7`, 보조 텍스트 `#5b6e63`, 경계 `#dce5dc`.
- 강조 butter `#e9c96a`. 위험은 muted red, 준비는 amber, 미확인은 slate blue. 색마다 상태명·아이콘을 함께 표시.
- 콘텐츠 최대 폭 1200px. 데스크톱에는 넉넉한 바깥 여백, 모바일은 20px.
- 버튼은 pill, 코스·사진은 20~32px 모서리. 모든 요소를 카드로 감싸지 않고 행·얇은 구분선·여백을 사용.
- 굵기 1.5~2px의 SVG 아이콘, 작은 발자국과 점선 경로를 반복되는 브랜드 요소로 사용.

## 한국어 타이포그래피

Pretendard Variable을 로컬 WOFF2로 제공하고 OFL 라이선스를 동봉한다. next/font/local로 외부 폰트 요청 없이 제공. 제목 700~800, 본문 400~500, 수치 600. 본문 16px/1.7, 판정 본문 13px 이상, 보조 안내 12px 기준. 짧은 장식 라벨은 별도로 작게 사용한다. 한글 단어 유지(word-break: keep-all), 장문 URL은 별도 줄바꿈. 모바일 제목은 clamp로 축소. 영문 로고는 같은 글꼴의 두꺼운 소문자와 자체 발자국 심벌.

## 화면과 실제 행동

1. `/`: 큰 한국어 제목, 반려견 사진, 곡선 코스와 상태 배지, 시작·체험 CTA, 세 단계 설명, 자주 묻는 질문.
2. `/plan`: 반려견 프로필·날짜·출발 시각·준비물, 장소 검색, 순서·체류·구역·고정 여부 편집. 데스크톱 2열, 모바일 1열.
3. 같은 작업 화면에서 검증 결과, 코스 노선도, 문제 요약. 장소 근거는 native dialog로 열어 키보드 포커스·Escape·복귀 지원.
4. 대체 후보를 비교하고 적용하면 전체 일정을 재검증. 이전 코스로 되돌리기 제공.
5. 코스와 연결된 준비표·미확인 문의 문구. 인쇄용 결과 제공.

가상 체험은 항상 `가상 체험` 배지를 표시하고 실제 장소·실시간 길찾기처럼 표현하지 않는다. 실제 모드에서 설정 누락·외부 오류가 나면 해당 상황을 안내하고 가상 결과로 자동 대체하지 않는다.

## 인터랙션

- 첫 화면의 400~650ms 진입, 요소별 짧은 시차. 코스 선·스티커는 절제된 움직임.
- 버튼 hover/focus, 조건 칩 선택, 결과 펼치기, 코스 순서 변경과 후보 교체는 즉각적인 피드백.
- 입력 변경 시 기존 결과에 `입력이 변경되었어요. 다시 검사해 주세요`를 표시하고 최신 결과로 오해하지 않게 한다.
- 요청 진행 중 상태와 중복 실행 방지, 오류 후 재시도, 빈 검색 결과 안내.
- prefers-reduced-motion에서는 장식 애니메이션 제거. 자동 스크롤 강제·스크롤 가로채기 없음.

## 접근성과 신뢰

44px 이상 주요 터치 영역, 보이는 focus ring, 명시적 label, aria-live 상태 안내, 키보드 순서 변경, native details/dialog. 상태를 색만으로 전달하지 않는다. 고정 요소가 모바일 내용을 가리지 않도록 여백 확보.

출처 원문은 텍스트로 렌더링하고 HTML을 실행하지 않는다. API 조회·콘텐츠 수정·규정 확인 시점을 구분. 실제 공사 데이터에는 `출처: ⓒ한국관광공사` 표기. 확률·가짜 후기·입장 보장·실측하지 않은 성과를 표시하지 않는다.

## 검증

Playwright로 데스크톱·모바일, 키보드·dialog·새로고침·프록시·배포 경로를 검증. 프로필 변경 → 가상 코스 판정 → 근거 → 대체 → 전체 재검증 → 준비표 흐름, 실제 모드 설정 누락·공급자 실패를 검사한다. 화면 스크린샷과 가로 넘침을 확인한다. 테스트는 스크립트로 재실행 가능하게 유지한다.

## 출처

- 시각 참고: 사용자 제공 ref/ref1.webp~ref4.webp (로컬 열람).
- 글꼴: https://github.com/orioncactus/pretendard (OFL).
- 제품 사진은 원본·제공처를 public/media/CREDITS.md에 기록한다.
