# PawProof 디자인 기준

2026-09-11 · 시각 디자인과 인터랙션의 기준. 제품 정책은 docs/product/, 구현 기록은 docs/delivery/를 따른다.

## 레퍼런스와 해석

사용자 제공 `ref/ref1.webp`~`ref4.webp`를 직접 확인했다. ref1의 세이지 바탕·짙은 녹색 제목·널찍한 여백, ref2의 모바일 흐름, ref3의 곡선 연결과 친근한 반려견, ref4의 색상·타입 체계를 참고한다. 원본의 로고·사진·실적·리뷰는 제품에 복사하지 않는다. 참고 이미지는 사용자 자료이며 이번 커밋에 포함하지 않는다.

방향: **숲빛 여행 노트**. 출발 전 여행 노트를 펼쳐 우리 강아지의 조건과 코스를 차분히 확인하는 경험. 신뢰를 주는 정보 밀도와 가볍고 따뜻한 여행 감성을 함께 만든다.

## 색과 형태

- 주색 forest `#2f6b50`, 깊은 잉크 `#193d30`, 세이지 `#eaf0e9`.
- 페이지 바탕 ivory `#fafbf7`, 보조 텍스트 `#5b6e63`, 경계 `#dce5dc`.
- 강조 butter `#e9c96a`. 위험은 muted red, 준비는 amber, 미확인은 slate blue. 색마다 상태명·아이콘을 함께 표시.
- 입력·선택·버튼의 포커스는 forest `#2f6b50` 테두리와 옅은 녹색 링으로 통일한다. 노란색/주황색은 준비 상태·장식에만 사용한다.
- 콘텐츠 최대 폭 1200px. 데스크톱에는 넉넉한 바깥 여백, 모바일은 20px.
- 버튼은 pill, 코스·사진은 20~32px 모서리. 모든 요소를 카드로 감싸지 않고 행·얇은 구분선·여백을 사용.
- 굵기 1.5~2px의 SVG 아이콘, 작은 발자국과 점선 경로를 반복되는 브랜드 요소로 사용.

## 한국어 타이포그래피

Pretendard Variable을 로컬 WOFF2로 제공하고 OFL 라이선스를 동봉한다. next/font/local로 외부 폰트 요청 없이 제공. 제목 700~800, 본문 400~500, 수치 600. 본문 16px/1.7, 판정 본문 13px 이상, 보조 안내 12px 기준. 짧은 장식 라벨은 별도로 작게 사용한다. 한글 단어 유지(word-break: keep-all), 장문 URL은 별도 줄바꿈. 모바일 제목은 clamp로 축소. 영문 로고는 같은 글꼴의 두꺼운 소문자와 자체 발자국 심벌.

## 화면과 실제 행동

1. `/`: 큰 한국어 제목, 반려견 사진, 곡선 코스와 상태 배지, 시작·체험 CTA, 세 단계 설명, 자주 묻는 질문.
2. `/plan`: 반려견 프로필·날짜·출발 시각·준비물, 장소 검색, 순서·체류·구역·고정 여부 편집. 데스크톱 2열, 모바일 1열.
3. 같은 작업 화면에서 검증 결과, 코스 노선도, 문제 요약. 장소 근거는 Radix Dialog로 열어 키보드 포커스·Escape·복귀 지원.
4. 대체 후보를 비교하고 적용하면 전체 일정을 재검증. 이전 코스로 되돌리기 제공.
5. 코스와 연결된 준비표·미확인 문의 문구. 인쇄용 결과 제공.

가상 체험은 항상 `가상 체험` 배지를 표시하고 실제 장소·실시간 길찾기처럼 표현하지 않는다. 실제 모드에서 설정 누락·외부 오류가 나면 해당 상황을 안내하고 가상 결과로 자동 대체하지 않는다.

## 인터랙션

- 2026-09-12 보완: 모든 Dialog는 불투명 흰색 표면·테두리·그림자와 모바일 여백을 공유한다. shadcn의 페이드·95% 확대 패턴을 CSS로 옮기고 Radix `data-state`와 닫힘 수명에 연결한다. 아코디언은 Radix가 측정한 높이로 펼치고 접는다.
- 홈 사진은 두 장의 아치형 여행 사진을 겹친다. 두 사진은 동일한 크기와 아치 반경을 공유한다. 마우스 진입 시 양옆으로 펼치고 이탈 시 복귀한다. 모바일·PWA에서는 사진을 탭해 펼치거나 접으며, 키보드 Enter/Space도 지원한다. reduced-motion에서는 전환 애니메이션만 없애고 펼침 기능은 유지한다. 영문 원형 도장은 줄별 span과 내부 여백으로 모든 화면에서 글자를 원 안에 유지한다.
- 첫 화면의 400~650ms 진입, 요소별 짧은 시차. 코스 선·스티커는 절제된 움직임.
- 버튼 hover/focus, 조건 칩 선택, 결과 펼치기, 코스 순서 변경과 후보 교체는 즉각적인 피드백.
- 입력 변경 시 기존 결과에 `입력이 변경되었어요. 다시 검사해 주세요`를 표시하고 최신 결과로 오해하지 않게 한다.
- 요청 진행 중 상태와 중복 실행 방지, 오류 후 재시도, 빈 검색 결과 안내.
- prefers-reduced-motion에서는 장식 애니메이션 제거. 자동 스크롤 강제·스크롤 가로채기 없음.

## 입력 컴포넌트 (2026-09-11 보완)

shadcn/ui의 Radix Select·Popover·Input 및 React DayPicker Calendar 소스를 기반으로 `src/components/ui/`에서 관리한다. 공식 조합·키보드·포털 구조를 사용하고, 스타일은 현재 CSS 토큰과 기존 아이콘에 맞게 조정한다. Tailwind 전체 도입은 필요하지 않다. 원본과 라이선스는 해당 폴더 README와 LICENSE에 기록한다.

- 체류시간·구역: 둥근 Select, 선택 항목 체크, 세이지 강조, 긴 목록 스크롤, Escape 닫기·포커스 복귀.
- 날짜: 한국어 달력 Popover, 월 이동 버튼, 선택 날짜 강조. 값은 기존 YYYY-MM-DD 계약을 유지하고 날짜 선택 시 닫는다.
- 첫 도착 시각: 24시간 기준 시·분 Select를 담은 Popover. 1분 단위 값을 보존하며 적용 버튼으로 확정한다. Escape/바깥 클릭은 미적용 값을 취소한다.
- 포털은 모바일 화면 경계를 고려하고 모든 입력은 처리 중 비활성화한다. 네이티브 select/date/time 팝업에 의존하지 않는다.

## 접근성과 신뢰

44px 이상 주요 터치 영역, 보이는 focus ring, 명시적 label, aria-live 상태 안내, 키보드 순서 변경, Radix Accordion과 Dialog. 상태를 색만으로 전달하지 않는다. 고정 요소가 모바일 내용을 가리지 않도록 여백 확보.

공통 컴포넌트·상태 책임은 [UI_DESIGN_SYSTEM.md](docs/engineering/UI_DESIGN_SYSTEM.md)에 기록한다. 개발 환경의 `/dev/design-system`에서 실제 버튼·입력·선택 상태를 확인한다.

출처 원문은 텍스트로 렌더링하고 HTML을 실행하지 않는다. API 조회·콘텐츠 수정·규정 확인 시점을 구분. 실제 공사 데이터에는 `출처: ⓒ한국관광공사` 표기. 확률·가짜 후기·입장 보장·실측하지 않은 성과를 표시하지 않는다.

## 검증

Playwright로 데스크톱·모바일, 키보드·dialog·새로고침·프록시·배포 경로를 검증. 프로필 변경 → 가상 코스 판정 → 근거 → 대체 → 전체 재검증 → 준비표 흐름, 실제 모드 설정 누락·공급자 실패를 검사한다. 화면 스크린샷과 가로 넘침을 확인한다. 테스트는 스크립트로 재실행 가능하게 유지한다.

## 출처

- 시각 참고: 사용자 제공 ref/ref1.webp~ref4.webp (로컬 열람).
- 글꼴: https://github.com/orioncactus/pretendard (OFL).
- 제품 사진은 원본·제공처를 public/media/CREDITS.md에 기록한다.

## 작업 알림

Sonner 알림도 Pretendard·페이퍼 배경·숲색 성공 아이콘·둥근 16px 경계를 사용한다. 실패는 붉은 아이콘과 명확한 문구로 구분하며 입력 포커스는 계속 초록색이다. 데스크톱 우측 상단, 모바일 좌우 16px와 상단 안전 영역 안에 표시해 하단 검사 버튼을 가리지 않는다. 최신 작업 하나만 표시하고 상세 결과는 화면에 남긴다. 모달보다 낮은 레이어, 40px 닫기 버튼, Alt+T 키보드 접근, reduced-motion, 인쇄 숨김을 적용한다.

## PWA 설치·연결·업데이트

기존 숲색·세이지·Pretendard를 유지한다. 헤더의 작은 ‘앱 설치’ 버튼에서 shadcn/Radix 안내 모달을 열고, 발바닥 아이콘을 홈 화면에서도 사용한다. 모바일 헤더의 간격을 조정해 주요 코스 버튼과 함께 표시한다. 설치 프롬프트가 없는 브라우저에는 수동 설치 방법을 안내한다. 오프라인과 새 버전 안내는 상단의 차분한 상태 띠로 표시한다. 업데이트는 저장 전 입력 손실을 설명하는 모달에서 사용자가 선택하며 강제 새로고침하지 않는다. 오프라인 새 접속 화면은 외부 폰트·이미지·스크립트 없이 읽을 수 있는 페이퍼 배경과 숲색으로 구성한다.


## 계정과 여행 노트 (2026-09-12)

헤더의 작은 로그인 버튼은 흰색 공통 Dialog를 연다. Google·이메일 로그인/가입/재설정은 같은 패널에서 처리하고 진행 중·인증 필요·실패를 명시한다. 모바일 헤더의 설치 버튼은 아이콘으로 축소한다. 여행 편집에는 별도 흰색 ‘계정 여행 노트’ 패널을 두며 기기 저장과 구분한다. 계정 노트는 제목·여행일·반려견/방문지 수와 불러오기·삭제 동작을 제공한다. 현재 입력 교체와 삭제는 확인 모달을 거치며 저장은 완료 후 Sonner로 알린다. 비로그인 코스 검사는 유지한다.

## 로그인·프로필·자동 저장 UX (2026-09-13)

로그인은 Google 버튼 → ‘또는 이메일로’ 구분선 → 이메일·비밀번호 → 로그인 버튼 → 작은 밑줄 회원가입/비밀번호 찾기 순서다. 가입은 10자 이상이며 성공 시 모달을 닫고 /plan으로 이동해 전역 토스트를 표시한다. /profile은 세이지 배경의 계정 요약과 흰색 반려견 등록 카드, 여행 노트로 돌아가는 링크를 제공한다. 등록된 반려견은 여행 입력에서 선택해 가져오되 기존 노트의 프로필을 소급 변경하지 않는다.

계정 노트는 편집 후 자동 저장 상태를 조용히 표시한다. 노트 전환·삭제는 저장 중 입력을 잃지 않게 처리한다. 저장된 장소명·일정·검사 요약을 복원하고 ‘저장 당시 검사 결과’ 시각을 표시한다. 원문·규정은 재검사 시 다시 조회한다. 검색 전 인천 탐색 후보와 최근 검색어를 보여주며 동반 가능 판정 전 후보임을 안내한다.
